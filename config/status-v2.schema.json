{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/tyong920/Infiniswap/config/status-v2.schema.json",
  "title": "Infiniswap Device Status v2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "kind",
    "device",
    "connection",
    "capacity",
    "mapping",
    "last_error"
  ],
  "properties": {
    "schema_version": {"const": 2},
    "kind": {"const": "infiniswap.device-status"},
    "device": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "path",
        "lifecycle",
        "mode",
        "acknowledgement_policy",
        "provider_failure_deadline_ms",
        "swap"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 31,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
        },
        "path": {"type": "string", "pattern": "^/dev/"},
        "lifecycle": {
          "enum": ["created", "active", "draining", "drained", "stopped"]
        },
        "mode": {"enum": ["backed", "remote-only", "unset"]},
        "acknowledgement_policy": {
          "enum": ["strict", "remote-first", "unset"]
        },
        "provider_failure_deadline_ms": {
          "type": "integer",
          "minimum": 500,
          "maximum": 30000
        },
        "swap": {
          "type": "object",
          "additionalProperties": false,
          "required": ["configured_priority", "enabled", "priority"],
          "properties": {
            "configured_priority": {
              "type": "integer",
              "minimum": 0,
              "maximum": 32767
            },
            "enabled": {"type": "boolean"},
            "priority": {
              "type": ["integer", "null"],
              "minimum": -1,
              "maximum": 32767
            }
          }
        }
      }
    },
    "connection": {
      "type": "object",
      "additionalProperties": false,
      "required": ["state", "total_providers", "healthy_providers", "providers"],
      "properties": {
        "state": {
          "enum": ["not-connected", "connecting", "connected", "degraded", "remote-lost"]
        },
        "total_providers": {"type": "integer", "minimum": 0, "maximum": 64},
        "healthy_providers": {"type": "integer", "minimum": 0, "maximum": 64},
        "providers": {
          "type": "array",
          "maxItems": 64,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["provider_id", "state"],
            "properties": {
              "provider_id": {"type": "string", "minLength": 1, "maxLength": 63},
              "state": {
                "enum": ["not-connected", "connecting", "connected", "degraded", "remote-lost"]
              }
            }
          }
        }
      }
    },
    "capacity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["advertised_bytes", "backing_bytes", "remote_bytes"],
      "properties": {
        "advertised_bytes": {"type": "integer", "minimum": 0},
        "backing_bytes": {"type": "integer", "minimum": 0},
        "remote_bytes": {"type": "integer", "minimum": 0}
      }
    },
    "mapping": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "chunk_size_bytes",
        "threshold",
        "read_weight",
        "write_weight",
        "mapped_hot_ranges"
      ],
      "properties": {
        "chunk_size_bytes": {"const": 1073741824},
        "threshold": {"type": "integer", "minimum": 1},
        "read_weight": {"type": "integer", "minimum": 1},
        "write_weight": {"type": "integer", "minimum": 1},
        "mapped_hot_ranges": {"type": "integer", "minimum": 0, "maximum": 128}
      }
    },
    "last_error": {
      "oneOf": [
        {"type": "null"},
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["code", "errno"],
          "properties": {
            "code": {"type": "string", "pattern": "^E[A-Z0-9_]+$"},
            "errno": {"type": "integer", "minimum": 1}
          }
        }
      ]
    }
  }
}
